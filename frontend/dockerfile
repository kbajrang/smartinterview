# frontend/Dockerfile
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy frontend source
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Run the frontend in dev mode with host binding
CMD ["npm", "run", "dev", "--", "--host"]
